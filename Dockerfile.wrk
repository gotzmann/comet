FROM ubuntu:19.10

RUN apt-get update && apt-get install -y \
    build-essential libssl-dev git \
    mc htop curl > /dev/null

RUN git clone https://github.com/wg/wrk.git wrk && \
    cd wrk && \
    make > /dev/null && \
    cp wrk /usr/local/bin

COPY post.lua /post.lua

CMD tail -f /dev/null
